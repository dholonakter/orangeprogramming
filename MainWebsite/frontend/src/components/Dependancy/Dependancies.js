import React from 'react'
import { useLocation } from "react-router";


const myContext = React.createContext();
const Papa = myContext.Provider
const SonData = myContext.Consumer









export {Papa, SonData}